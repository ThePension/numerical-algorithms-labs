function setup() {
    // Create the canevas
    createCanvas(windowWidth, 700);
    background(200);

    // Draw a splitter line
    translate(width / 2, 0);
    stroke(0);
    line(0, 0, 0, height);
}

function draw() {
    // TODO
}